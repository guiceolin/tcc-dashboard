- form_for @membership, :url => {:controller => :memberships, :action => :update, :project_id => @membership.project }, :method => :put do |f|
  = @membership.user.full_name
  %br
  = check_box_tag 'membership[manager]', true, @membership.manager?
  Gerente?
  %br
  = f.submit

