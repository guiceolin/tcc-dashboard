.registration-box
  %h2 Editar
  %p.subtitle edite os dados do seu cadastro.
  = simple_form_for(resource, :as => resource_name, :url => edit_member_registration_path(resource) ,:html => { :method => :put }) do |f|
    = devise_error_messages!
    = f.input :first_name
    = f.input :last_name
    = f.input :email
    = f.input :password, :required => false
    = f.input :password_confirmation, :required => false
    %i (we need your current password to confirm your changes)
    = f.input :current_password, :as => :password

    = f.button :submit, "Atualizar"
